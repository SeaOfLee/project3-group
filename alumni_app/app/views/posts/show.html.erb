<h1>Posts#show</h1>
<p>Find me in app/views/posts/show.html.erb</p>

    <h2><%= @post.title %></h2>
    <p>Published <%= time_ago_in_words(@post.created_at) %> ago by User<br />

    <p><%= simple_format(@post.content) %></p>

    <%= link_to "Edit", edit_post_path(@post) %>
    <%= link_to "Delete", post_path(@post), method: :delete, data: { confirm: "Are you sure you want to do this? Be warned, it's permanent!" }%>

    <h5># replies</h4>
      <%= render @post.comments %>
      <h4>Reply to question</h4>
      <%= render 'comments/form' %>
