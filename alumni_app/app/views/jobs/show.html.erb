<h1>Jobs#show</h1>
<p>Find me in app/views/jobs/show.html.erb</p>
<p>Posted <%= time_ago_in_words(@job.created_at) %> ago by User<br />

<h2><%= @job.job_title %></h2>
<p><%= @job.company %></p>
<p><%= @job.location %></p>
<p><%= @job.job_type %></p>
<p><%= @job.description %></p>
<p><%= mail_to @job.contact %></a></p>

  <%= link_to "Edit", edit_job_path(@job) %>
  <%= link_to "Delete", job_path(@job), method: :delete, data: { confirm: "Are you sure you want to delete this?" }%>
